# Roadmap

- Una sola pàgina (single-page) amb menú de navegació per àncores.

001-pagina-inicial
Estat: completada.

La pàgina de la revista NutriVida completa: capçalera amb data, logo i menú responsiu (hamburguesa al mòbil); hero amb article destacat; seccions de consells, superaliments, receptes i rutines de temporada amb tabs CSS. Mode clar per defecte amb botó per canviar a mode fosc (persistència via localStorage).

Especificació: spec/features/001-pagina-inicial/.

002-modo-oscuro
Estat: pendent.

Millorar l'experiència de mode fosc si cal. Ja implementat dins de 001 amb JS + localStorage.

Especificació: spec/features/002-modo-oscuro/.
